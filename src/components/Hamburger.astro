---
// src/components/Hamburger.astro
---
<div class="hamburger">
  <span class="line"></span>
  <span class="line"></span>
  <span class="line"></span>
</div>

<style>
  .hamburger {
    padding: 10px; /* 增加點擊範圍 */
    cursor: pointer;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    width: 40px;  /* 按鈕總寬度 */
    height: 35px; /* 按鈕總高度 */
  }

  .hamburger .line {
    display: block;
    width: 100%;
    height: 3px; /* 線條變細一點 */
    background-color: #333; /* 改成深灰色，不要橘色 */
    border-radius: 4px; /* 圓角看起來比較現代 */
    transition: all 0.3s ease-in-out;
  }

  /* 平板與電腦版隱藏漢堡 */
  @media screen and (min-width: 636px) {
    .hamburger {
      display: none;
    }
  }
</style>